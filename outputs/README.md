# Outputs Folder

This folder is created at runtime for generated output files.

Common generated subfolders:

```text
outputs/unknown_faces/
outputs/prototype_slides/
```

Generated outputs are not uploaded to GitHub because they may contain uploaded suspect images or temporary files.
