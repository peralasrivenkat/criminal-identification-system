# Models Folder

This folder contains the model training helper script and local trained artifacts.

Included in GitHub:

```text
train_models.py
facenet/README.md
```

Generated locally after training:

```text
scaler.pkl
pca_model.pkl
aco_features.pkl
mlp_model.pkl
centroid_model.pkl
exemplar_model.pkl
training_metadata.json
```

These generated files are intentionally ignored because they are dataset-specific runtime artifacts.
